# Speech-to-Text Backend Environment Variables

# Server Configuration
PORT=3001
NODE_ENV=development

# Google Cloud Speech-to-Text (Optional - if not provided, will use Deepgram)
GOOGLE_APPLICATION_CREDENTIALS=path/to/your/google-credentials.json
GOOGLE_CLOUD_PROJECT_ID=your-google-cloud-project-id

# Deepgram API (Required if Google Cloud is not configured)
DEEPGRAM_API_KEY=your-deepgram-api-key

# Supabase (Optional - for storing transcriptions)
SUPABASE_URL=your-supabase-url
SUPABASE_ANON_KEY=your-supabase-anon-key

# CORS Configuration
ALLOWED_ORIGINS=https://your-frontend-domain.com,http://localhost:3000